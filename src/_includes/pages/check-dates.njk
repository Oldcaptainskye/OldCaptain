<section id="book" class="book"><div class="wrap">
  <h1 style="color:#fff;font-size:clamp(1.8rem,3.4vw,2.3rem)">{{ T.ui.checkDates }}</h1>
  <p>{{ T.ui.directLine }}</p>
  <div class="booking-frame">
    <iframe src="{{ site.bookingUrl }}" title="{{ T.ui.checkDates }} — availability and booking" loading="lazy"></iframe>
  </div>
</div></section>
